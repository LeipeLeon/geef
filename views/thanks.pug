extends layout

block content
  h1= title
  #thanks
    img(src="images/pol" + number + ".gif" alt="Image Title")
  h2 en veel plezier met de optocht!
  form(method='GET' action='/')
    button(name='return' value='thanks' class="btn") Doneer nog een keer!

